import { ThumbsUp, ThumbsDown, HourglassIcon } from "lucide-react";

const EvidenceListPanel = ({ selectedMember, evidenceItems, statusFilter, setStatusFilter, handleEvidenceClick, evidenceStats, formatDate, mapStatusToDisplay, getEvidenceStatusIcon, getEvidenceStatusColor }) => {
    return (
        <div className="w-full md:w-1/2 bg-white rounded-lg shadow overflow-hidden">
            {/* Header + filter badge */}
            {/* Stats grid */}
            {/* Table rendering (map evidenceItems) */}
        </div>
    );
};

export default EvidenceListPanel;